<h1>Search Results</h1>
<p>Query: <i>{{{data.query}}}</i></p>
<div class="table-wrapper">
	<table>
		<thead>
			<tr>
				<th>Gene ID</th>
				<th>Gene Symbol</th>
				<th>Description</th>
			</tr>
		</thead>
		<tbody>
			{{#each data.geneData}}
				<tr>
					{{#each this}}
						<td>{{{this}}}</td>
					{{/each}}
				</tr>
			{{/each}}
		</tbody>
	</table>
</div>
